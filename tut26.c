/*
Author : Anas khan
DATE  : 12-09-22
Purpose : Learning C
TOPIC  : Pointer wiht array arithmeteic
*/
#include <stdio.h>
int main(int argc, char const *argv[]) {
  for (size_t i = 0; i < count; i++) {

    /* code */
  }
  // char a= 43;
  // char* ptra = &a;
  // printf("%d\n",ptra);
  // ptra++;
  // printf("%d\n",ptra);
  // printf("%p",ptra+4 );
  // int array[4]={4,25,3,2};
  // int* ar= &array;
  // printf("%d\n",&array[0] );
  // printf("%d\n",ar );
  // printf("%d\n",sizeof(int) );
  // int arr[]= {2,3,4,3};
  // printf("Value at positon 3 of the array is %d\n",arr[3] );
  // printf("The address of first element of the array%d\n",&arr[0]);
  // printf("The address of second elemtnt of the array is %d \n",&arr[1] );
  // printf("The addres of second elment of the array %d\n",arr+2);
  // int arrau[]={5,2,5,23};
  // printf("%d\n",&arrau );
  //
  // int* poin= arrau;
  //
  // printf("The address of the first element of list %d\n",arrau+1 );
  // poin++;
  // printf("The address of the second element of list %d\n",&arrau[1] );
  // printf("The address of the third element of list %d\n",&arrau[2] );
  // printf("The address of the fourht  element of list %d\n",&arrau[3] );
  //
  // char a = 2;
  // char* point = &a;
  // printf("%d\n",&a );
  // point++;
  // printf("%d\n",&a );

  // int a = 34;           yeh cheeze bytes le sakte hai address change karne
  // int* point = &a;     ke liye kaam mai aati hai + karke kar sakte hai
  // printf("%d\n",&a );
  // point++;
  // printf("%d\n",&point+1 );

  // printf("Value at positon 3 of the array is %d\n",*(arr[3] ));
  // printf("The address of first element of the array%d\n",*(&arr[0]));
  // printf("The address of second elemtnt of the array is %d \n",*(&arr[1] ));
  // printf("The addres of second elment of the array %d\n",*(arr+2) );
  // return 0;
}

// **Harry's Code **

// #include <stdio.h>
// int main()
// {
//     // char a = '3';
//     // char* ptra = &a;
//     // printf("%d\n", ptra);
//     // ptra--;
//     // printf("%d\n", ptra);
//     // printf("%d", ptra-2);
//     int arr[] = {311,52,3,4,5,6,67};
//     int* arrayptr = arr;
//     printf("Value at position 3 of the array is %d\n", arr[3]);
//     printf("The address of first element of the array is %d \n", &arr[0]);
//     printf("The address of first element of the array is %d \n", arr);
//     printf("The address of second element of the array is %d \n", &arr[1]);
//     printf("The address of second element of the array is %d \n", arr + 1);
//     printf("The address of third element of the array is %d \n", &arr[2]);
//     printf("The address of third element of the array is %d \n", arr + 2);
//     // arr--; // this line will throw an error
//
//     printf("The value at address of first element of the array is %d \n", *(&arr[0]));
//     printf("The value at address of first element of the array is %d \n", arr[0]);
//     printf("The value at address of first element of the array is %d \n", *(arr));
//     printf("The value at address of second element of the array is %d \n", *(&arr[1]));
//     printf("The value at address of second element of the array is %d \n", arr[1]);
//     printf("The value at address of second element of the array is %d \n", *(arr + 1));
//
//     return 0;
// }
